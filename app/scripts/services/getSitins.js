// 'use strict';

// /* global Firebase */
// angular.module('fullsailsitinApp')
// 	.factory('GetSitins', ['$rootScope','$firebase', function ($rootScope, $firebase){

// 			// var fb = new Firebase('https://sitin.firebaseio.com/users/' + $rootScope.currentUser.id + '/attended');


// 			// $rootScope.sitins = $firebase(fb);
// 			console.lo

// 			// var userAttended = [];

// 			// //Apparently 'change' gives the item's key
// 			// //Rachel wrote this.
// 			// attended.$on('change',function(changed)
// 			// {

// 			// 		if(attended[changed].user === $rootScope.currentUser.name){
// 			// 			userAttended.push(attended[changed]);
// 			// 		}


// 			// 		$rootScope.currentUser.sitins = userAttended.length;
// 			// 		$rootScope.sitins = userAttended;

// 			// 	});
// 		}]);
